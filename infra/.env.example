# Copia este archivo a ".env" en el servidor (la EC2) y pon los valores reales.
# NUNCA subas el ".env" real a GitHub -- por eso está en .gitignore.

POSTGRES_PASSWORD=CAMBIA_ESTO_por_openssl_rand_hex_16
S3_BUCKET=nlp-lab2-sentimientos
AWS_REGION=us-east-1
