services:
  postgres:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_DB: mlflow
      POSTGRES_USER: mlflow
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD}"
    volumes:
      - pgdata:/var/lib/postgresql/data

  mlflow:
    build:
      context: .
      dockerfile: Dockerfile.mlflow
    restart: always
    depends_on:
      - postgres
    ports:
      - "5000:5000"
    environment:
      AWS_DEFAULT_REGION: "${AWS_REGION:-us-east-1}"
    command: ["mlflow", "server",
      "--backend-store-uri", "postgresql://mlflow:${POSTGRES_PASSWORD}@postgres:5432/mlflow",
      "--default-artifact-root", "s3://${S3_BUCKET}/mlflow-artifacts",
      "--host", "0.0.0.0",
      "--port", "5000",
      "--serve-artifacts"]

  # ---------------------------------------------------------------
  # La API FastAPI se agrega aquí cuando exista la imagen (Anexo A.5).
  # api:
  #   build: ./api
  #   restart: always
  #   depends_on:
  #     - mlflow
  #   ports:
  #     - "8000:8000"
  #   environment:
  #     MLFLOW_TRACKING_URI: "http://mlflow:5000"
  #     AWS_DEFAULT_REGION: "${AWS_REGION:-us-east-1}"
  # ---------------------------------------------------------------

volumes:
  pgdata:
